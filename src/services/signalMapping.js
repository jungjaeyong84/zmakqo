const {
  SIGNAL_MAPPING_VERSION,
  normalizeEvent,
  normalizeSide,
  mapEventToIntentSide,
  resolveEventMapping,
} = require("./signalStandard");

module.exports = {
  SIGNAL_MAPPING_VERSION,
  normalizeEvent,
  normalizeSide,
  mapEventToIntentSide,
  resolveEventMapping,
};
