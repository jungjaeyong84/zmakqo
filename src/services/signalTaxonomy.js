const {
  normalizeStr,
  normalizeSide,
  groupEvent,
  subtypeOf,
  deriveGroupSubtype,
  makeDropKey,
} = require("./signalStandard");

module.exports = {
  normalizeStr,
  normalizeSide,
  groupEvent,
  subtypeOf,
  deriveGroupSubtype,
  makeDropKey,
};
