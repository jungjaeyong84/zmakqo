<%
  const __active = (typeof active !== 'undefined' && active) ? String(active) : 'mission';
  const __links = [
    { key: 'mission', href: '/dashboard/home', label: 'V2 미션' },
    { key: 'recovery', href: '/dashboard/recovery', label: '증거 수집' },
    { key: 'deployment', href: '/dashboard/deployment', label: '승격 판정' },
    { key: 'execution', href: '/dashboard/execution', label: '실행 추적' },
    { key: 'server-primary', href: '/dashboard/server-primary', label: 'OpenClaw 전환' },
    { key: 'audit', href: '/dashboard/audit', label: 'V2 감사' },
  ];
%>
<div class="subnav" style="margin-top:-4px; margin-bottom:18px;">
  <% __links.forEach((link) => { %>
    <a href="<%= link.href %>" class="<%= __active === link.key ? 'active' : '' %>"><%= link.label %></a>
  <% }) %>
</div>
