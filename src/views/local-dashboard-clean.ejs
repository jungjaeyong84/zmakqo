<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>DONBEOLJA</title>
<meta http-equiv="refresh" content="60">
<style>
  :root{
    --bg:#0f1115; --panel:#171a21; --panel2:#1d212a; --line:#272c38;
    --fg:#e6e9ef; --dim:#9aa3b2; --faint:#6b7484;
    --ok:#3ecf8e; --up:#3ecf8e; --down:#f0616d; --warn:#f5a524; --bad:#f0616d;
  }
  @media (prefers-color-scheme: light){
    :root{ --bg:#f6f7f9; --panel:#fff; --panel2:#f0f2f5; --line:#e0e3e9;
           --fg:#16191f; --dim:#5b6472; --faint:#8a93a3; }
  }
  *{box-sizing:border-box}
  body{margin:0;background:var(--bg);color:var(--fg);
    font:14px/1.5 -apple-system,BlinkMacSystemFont,"Apple SD Gothic Neo",Pretendard,sans-serif}
  .wrap{max-width:760px;margin:0 auto;padding:24px 16px 56px}

  .top{display:flex;align-items:baseline;gap:10px;margin-bottom:16px}
  h1{font-size:19px;margin:0;letter-spacing:.5px}
  .stamp{margin-left:auto;color:var(--faint);font-size:11.5px;font-variant-numeric:tabular-nums}

  /* 상태 스트립 — 사실 4개, 설명 없음 */
  .strip{display:grid;grid-template-columns:repeat(4,1fr);gap:1px;background:var(--line);
    border:1px solid var(--line);border-radius:10px;overflow:hidden;margin-bottom:18px}
  .strip div{background:var(--panel);padding:10px 12px}
  .strip .k{color:var(--faint);font-size:11px}
  .strip .v{font-size:15px;font-weight:600;margin-top:2px}
  .ok,.up{color:var(--up)} .down,.bad{color:var(--down)} .warn{color:var(--warn)}

  /* 메인 카드 */
  .main{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:20px;margin-bottom:16px}
  .main h2{font-size:15px;margin:0 0 2px;display:flex;align-items:center;gap:8px}
  .badge{font-size:11px;padding:3px 9px;border-radius:999px;background:var(--panel2);
    border:1px solid var(--line);color:var(--dim);font-weight:500}
  .badge.up{color:var(--up)} .badge.down{color:var(--down)}
  .cfg{color:var(--faint);font-size:11.5px;margin:0 0 16px;font-variant-numeric:tabular-nums}

  .big{display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:14px;margin-bottom:16px}
  .big div .k{color:var(--faint);font-size:11px}
  .big div .v{font-size:26px;font-weight:650;letter-spacing:-.5px;margin-top:3px;
    font-variant-numeric:tabular-nums}
  .big div .s{color:var(--faint);font-size:11px;margin-top:1px}

  .bar{height:7px;background:var(--panel2);border:1px solid var(--line);border-radius:99px;
    overflow:hidden;margin:4px 0 6px}
  .bar>i{display:block;height:100%;background:var(--up)}
  .barnote{color:var(--faint);font-size:11.5px;margin-bottom:16px}

  table{width:100%;border-collapse:collapse;font-size:12.5px;font-variant-numeric:tabular-nums}
  th,td{text-align:right;padding:6px 8px;border-bottom:1px solid var(--line);white-space:nowrap}
  th{color:var(--faint);font-weight:500;font-size:11px}
  th:first-child,td:first-child{text-align:left}
  tbody tr:last-child td{border-bottom:none}
  .tblwrap{overflow-x:auto}
  .sub{color:var(--dim);font-size:12px;margin:16px 0 6px;font-weight:600}
  .mut{color:var(--faint)}

  /* 나머지 레인 — 한 줄씩 */
  .others{background:var(--panel);border:1px solid var(--line);border-radius:12px;overflow:hidden}
  .row{display:flex;align-items:center;gap:12px;padding:11px 16px;border-bottom:1px solid var(--line)}
  .row:last-child{border-bottom:none}
  .row .n{font-size:13px;min-width:118px}
  .row .st{font-size:13px;font-weight:600;font-variant-numeric:tabular-nums}
  .row .nt{color:var(--faint);font-size:11.5px;margin-left:auto;text-align:right}
  /* On a phone the three columns collide and every value wraps mid-word.
     Stack instead: name+value on one line, the note underneath. */
  @media (max-width: 560px){
    .row{flex-wrap:wrap;row-gap:3px}
    .row .n{min-width:0;flex:1}
    .row .st{white-space:nowrap}
    .row .nt{flex-basis:100%;margin-left:0;text-align:left}
    .strip{grid-template-columns:repeat(2,1fr)}
    .big{grid-template-columns:repeat(2,1fr)}
  }
  .empty{color:var(--faint);font-size:12.5px;padding:14px 0;text-align:center}
  footer{color:var(--faint);font-size:11px;text-align:center;margin-top:20px}
</style>
</head>
<body>
<div class="wrap">

  <div class="top">
    <h1>DONBEOLJA</h1>
    <span class="stamp"><%= helpers.formatDateTime(generatedAt) %></span>
  </div>

  <div class="strip">
    <% strip.forEach(function(s){ %>
      <div><div class="k"><%= s.label %></div><div class="v <%= s.tone %>"><%= s.value %></div></div>
    <% }); %>
  </div>

  <div class="main">
    <h2>
      v6 페이퍼
      <span class="badge <%= v6.tone %>"><%= v6.verdictLabel %></span>
    </h2>
    <p class="cfg">
      td+vol 조합 · 임계값 <%= v6.config.threshold %> · 선물 <%= v6.leverage %>배
      · 익절 <%= helpers.formatSigned(v6.tpPct, 1, '%') %> / 손절 <%= helpers.formatSigned(v6.slPct, 1, '%') %>
      · 최대 <%= v6.config.hold_hours %>시간 · 비용 <%= v6.config.cost_pct %>% 반영
      <% if (v6.caps) { %>
        <br>동시 <%= v6.caps.max_concurrent %>건 · 한쪽 <%= v6.caps.max_per_side %>건 · 회당 신규 <%= v6.caps.max_new_per_cycle %>건 상한
      <% } %>
    </p>

    <% if (v6.byPolicy.length > 1) { %>
      <div class="tblwrap" style="margin-bottom:14px"><table>
        <thead><tr><th>선정 정책</th><th>거래</th><th>승률</th><th>누적 순%</th><th>t</th></tr></thead>
        <tbody>
          <% v6.byPolicy.forEach(function(p){ %>
            <tr>
              <td><%= p.policy_version === 1 ? 'v1 (상한 없음)' : 'v2 (상한 적용)' %></td>
              <td class="mut"><%= p.closed_n %></td>
              <td class="mut"><%= helpers.formatNumber(p.win_rate_pct, 1) %>%</td>
              <td class="<%= p.total_net_pct >= 0 ? 'up' : 'down' %>"><%= helpers.formatSigned(p.total_net_pct, 2, '%') %></td>
              <td class="mut"><%= p.t_stat === null ? '-' : helpers.formatNumber(p.t_stat, 2) %></td>
            </tr>
          <% }); %>
        </tbody>
      </table></div>
    <% } %>

    <div class="big">
      <div>
        <div class="k">종료 거래</div>
        <div class="v"><%= helpers.formatNumber(v6.closedN) %></div>
        <div class="s">보유 중 <%= helpers.formatNumber(v6.openN) %>건</div>
      </div>
      <% if (v6.realised) { %>
        <div>
          <div class="k">승률</div>
          <div class="v"><%= helpers.formatNumber(v6.realised.win_rate_pct, 1) %>%</div>
          <div class="s">익절 <%= v6.realised.exit_target_n %> · 손절 <%= v6.realised.exit_stop_n %> · 만기 <%= v6.realised.exit_expiry_n %></div>
        </div>
        <div>
          <div class="k">누적 순손익</div>
          <div class="v <%= v6.realised.total_net_pct >= 0 ? 'up' : 'down' %>">
            <%= helpers.formatSigned(v6.realised.total_net_pct, 2, '%') %>
          </div>
          <div class="s">거래당 <%= helpers.formatSigned(v6.realised.mean_net_pct, 3, '%') %></div>
        </div>
        <div>
          <div class="k">t 값</div>
          <div class="v <%= v6.realised.t_stat > 1.96 ? 'up' : '' %>">
            <%= v6.realised.t_stat === null ? '-' : helpers.formatNumber(v6.realised.t_stat, 2) %>
          </div>
          <div class="s">1.96 넘어야 유의</div>
        </div>
      <% } else { %>
        <div>
          <div class="k">승률</div><div class="v mut">-</div><div class="s">종료 거래 없음</div>
        </div>
      <% } %>
    </div>

    <div class="bar"><i style="width:<%= v6.progressPct %>%"></i></div>
    <div class="barnote">
      판정까지 <%= helpers.formatNumber(v6.closedN) %> / <%= helpers.formatNumber(v6.minN) %> 거래
      · <%= helpers.formatNumber(v6.daysRunning, 1) %>일 경과
      <%
        /* The backtest's RETURN is the seductive number and it would be read as
           a forecast. Its t-stat is the honest one: this config reached t 1.10,
           below the 3.39 that a shuffled null reaches in the same search. Show
           the number that limits the claim, not the one that inflates it. */
      %>
      <% if (v6.expectedT !== null) { %>
        · 백테스트 t <%= helpers.formatNumber(v6.expectedT, 2) %> (유의 기준 1.96 미달 — 검증 아님)
      <% } %>
    </div>

    <% if (v6.open.length) { %>
      <div class="sub">보유 중</div>
      <div class="tblwrap"><table>
        <thead><tr><th>심볼</th><th>방향</th><th>진입가</th><th>익절가</th><th>손절가</th><th>만기</th></tr></thead>
        <tbody>
          <% v6.open.forEach(function(p){ %>
            <tr>
              <td><%= p.symbol.replace('USDT','') %></td>
              <td class="<%= p.side === 'LONG' ? 'up' : 'down' %>"><%= p.side %></td>
              <td class="mut"><%= helpers.formatPrice(p.entry_price) %></td>
              <td class="up"><%= helpers.formatPrice(p.tp_price) %></td>
              <td class="down"><%= helpers.formatPrice(p.sl_price) %></td>
              <td class="mut"><%= helpers.formatDateTime(p.close_due_at) %></td>
            </tr>
          <% }); %>
        </tbody>
      </table></div>
    <% } %>

    <% if (v6.recent.length) { %>
      <div class="sub">최근 종료</div>
      <div class="tblwrap"><table>
        <thead><tr><th>심볼</th><th>방향</th><th>가격%</th><th>레버후%</th><th>순%</th><th>사유</th></tr></thead>
        <tbody>
          <% v6.recent.forEach(function(p){ %>
            <tr>
              <td><%= p.symbol.replace('USDT','') %></td>
              <td class="<%= p.side === 'LONG' ? 'up' : 'down' %>"><%= p.side %></td>
              <td class="mut"><%= helpers.formatSigned(p.price_move_pct, 2, '%') %></td>
              <td class="mut"><%= helpers.formatSigned(p.gross_pct, 2, '%') %></td>
              <td class="<%= p.net_pct >= 0 ? 'up' : 'down' %>"><%= helpers.formatSigned(p.net_pct, 2, '%') %></td>
              <td class="mut"><%= p.exit_reason === 'TARGET' ? '익절' : p.exit_reason === 'STOP' ? '손절' : '만기' %></td>
            </tr>
          <% }); %>
        </tbody>
      </table></div>
    <% } %>

    <% if (!v6.open.length && !v6.recent.length) { %>
      <div class="empty">아직 신호가 발생하지 않았습니다. 매시간 24개 심볼을 확인합니다.</div>
    <% } %>
  </div>

  <div class="others">
    <% others.forEach(function(o){ %>
      <div class="row">
        <span class="n mut"><%= o.name %></span>
        <span class="st"><%= o.state %></span>
        <span class="nt"><%= o.note %> · <%= helpers.formatAgo(o.at) %></span>
      </div>
    <% }); %>
  </div>

  <footer>실거래 0원 · 전 레인 페이퍼 · <%= header.refreshNote %></footer>
</div>
</body>
</html>
